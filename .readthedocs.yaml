version: 2

build:
  os: ubuntu-22.04
  tools:
    python: "3.10"

python:
  install:
    - requirements: docs/requirements.txt

sphinx:
  configuration: docs/conf.py

# Optional: Build documentation from Markdown files
# formats:
#   - htmlzip
#   - pdf
#   - epub
